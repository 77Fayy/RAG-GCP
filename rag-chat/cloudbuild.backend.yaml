steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
    [
      'build',
      '-t', 'us-central1-docker.pkg.dev/$PROJECT_ID/rag-repo/rag-backend:latest',
      '-f', 'Dockerfile.backend',
      '.'
    ]

images:
- 'us-central1-docker.pkg.dev/$PROJECT_ID/rag-repo/rag-backend:latest'

